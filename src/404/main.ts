/*
User types URL/article_name
website returns page 404
html script gets article_name
sends request to GCP (or local cache)
if the article is there -> redirects to article.html (?)
and rewrites the URL to the original one
if the article is not there -> renders a custom 404 page

*/